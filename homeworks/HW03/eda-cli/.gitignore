# =============================================================================
# .gitignore для проекта EDA CLI (HW03)
# =============================================================================
# Данный файл определяет правила исключения файлов и директорий из системы
# контроля версий Git в соответствии с требованиями задания HW03.
# =============================================================================

# -----------------------------------------------------------------------------
# Виртуальное окружение Python
# -----------------------------------------------------------------------------
# Исключаются директории виртуального окружения, создаваемые различными
# инструментами управления зависимостями (venv, virtualenv, uv).
.venv/
venv/
env/
ENV/

# -----------------------------------------------------------------------------
# Сгенерированные отчёты
# -----------------------------------------------------------------------------
# Исключаются все директории с результатами выполнения команды report,
# так как они являются временными артефактами и могут быть воспроизведены.
reports/
reports_*/
reports_example/
reports_custom/
reports_pycharm/

# -----------------------------------------------------------------------------
# Python: скомпилированные файлы и кеш
# -----------------------------------------------------------------------------
# Исключаются байт-код файлы и директории кеширования интерпретатора Python.
*.pyc
*.pyo
*.pyd
__pycache__/
*.py[cod]
*$py.class
*.so

# -----------------------------------------------------------------------------
# Тестирование и покрытие кода
# -----------------------------------------------------------------------------
# Исключаются артефакты работы pytest, coverage и других инструментов
# тестирования и анализа покрытия кода.
.pytest_cache/
.coverage
.coverage.*
htmlcov/
.tox/
.nox/
coverage.xml
*.cover
.hypothesis/
.pytype/

# -----------------------------------------------------------------------------
# Сборка и дистрибуция пакета
# -----------------------------------------------------------------------------
# Исключаются директории и файлы, создаваемые при сборке и установке пакета.
*.egg-info/
*.egg
dist/
build/
wheels/
pip-wheel-metadata/

# -----------------------------------------------------------------------------
# UV package manager
# -----------------------------------------------------------------------------
# Файл uv.lock включается в репозиторий для обеспечения воспроизводимости
# окружения. Раскомментируйте следующую строку только в случае намеренного
# исключения lockfile из системы контроля версий.
# uv.lock

# -----------------------------------------------------------------------------
# Интегрированные среды разработки (IDE)
# -----------------------------------------------------------------------------
# Исключаются файлы конфигурации и кеша популярных IDE.
# PyCharm
.idea/
*.iml
*.ipr
*.iws

# Visual Studio Code
.vscode/
*.code-workspace

# Sublime Text
*.sublime-project
*.sublime-workspace

# Jupyter Notebook
.ipynb_checkpoints/
*.ipynb

# -----------------------------------------------------------------------------
# Временные и служебные файлы
# -----------------------------------------------------------------------------
# Исключаются временные файлы редакторов, логи и backup-файлы.
*.tmp
*.temp
*.log
*.swp
*.swo
*~
*.bak
*.orig

# -----------------------------------------------------------------------------
# Операционная система
# -----------------------------------------------------------------------------
# Исключаются системные файлы, создаваемые операционными системами.
# macOS
.DS_Store
.AppleDouble
.LSOverride

# Windows
Thumbs.db
Thumbs.db:encryptable
ehthumbs.db
ehthumbs_vista.db
Desktop.ini
$RECYCLE.BIN/

# Linux
.directory
.Trash-*

# -----------------------------------------------------------------------------
# Данные (datasets)
# -----------------------------------------------------------------------------
# Исключаются все крупные датасеты из директории data/, за исключением
# учебного файла example.csv, который должен присутствовать в репозитории
# согласно требованиям задания.
data/*.csv
data/*.xlsx
data/*.xls
data/*.json
data/*.parquet
data/*.feather
!data/example.csv

# Исключение архивов с данными
data/*.zip
data/*.tar.gz
data/*.7z

# -----------------------------------------------------------------------------
# Дополнительные исключения
# -----------------------------------------------------------------------------
# Прочие файлы, которые не должны попадать в репозиторий.
# Локальные конфигурационные файлы
.env
.env.local
*.local

# Файлы документации (если генерируются автоматически)
docs/_build/
site/

# Профилирование
*.prof
*.lprof